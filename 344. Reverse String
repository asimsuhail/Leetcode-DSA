class Solution {
public:
    void reverseString(vector<char>& s) {
        int size = s.size() - 1;
        int i = 0;
        while (i < size) {
            std::swap(s[i], s[size]);
            ++i;
            --size;
        }
    }
};
